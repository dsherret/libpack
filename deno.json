{
  "lock": false,
  "tasks": {
    "build": "mkdir -p dist && cp lib/rs_lib_bg.wasm dist/rs_lib_bg.wasm && cp main.ts dist/main.ts && deno run -A main.ts mod.ts dist && deno check dist/main.ts",
    "wasmbuild": "deno run -A https://deno.land/x/wasmbuild@0.14.1/main.ts"
  },
  "exclude": [
    "target",
    "lib"
  ]
}
